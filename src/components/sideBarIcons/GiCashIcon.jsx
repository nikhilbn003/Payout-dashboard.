import React from "react";
import { GiCash as GiCashIcon } from "react-icons/gi";

const CustomGiCash = ({ size, className }) => {
  return <GiCashIcon size={size} className={className} />;
};

export default CustomGiCash;
